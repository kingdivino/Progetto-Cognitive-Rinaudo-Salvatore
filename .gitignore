# Ambiente Python
venv/
.venv/
__pycache__/
*.pyc

# Chiavi e segreti — MAI committare
.env

# Dati grezzi pesanti (se troppo grandi per git, meglio non versionarli)
data/raw/*
!data/raw/.gitkeep

# Indice RAG (FAISS + metadata) - artefatto rigenerabile con
# notebooks/05_build_rag_index.py, 14MB, non versionato per lo stesso motivo di data/raw
data/rag/*
!data/rag/.gitkeep

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Output di debug degli script, non va versionato
scrape_output.txt
debug_*.html

# Checkpoint del fine-tuning (LoRA, ottimizzatore, tokenizer per ogni run/epoca) -
# 467MB in totale, ben oltre il limite pratico di GitHub senza Git LFS, e comunque
# rigenerabili con notebooks/13_finetune_train.py. Si tiene solo il CSV con i dati
# reali (finetune_dataset.csv), che serve anche a stats_tool.py in produzione.
data/processed/finetune_powerlevel/
